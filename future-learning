the future{
gulp构建工具
sass
react
sass should be the first
Installs n, the Node.js version manager
compass
gem install compass 出问题
}
学习资料｛
src https://css-tricks.com
在实现两栏布局时，如果用float则可以做到布局对内容自适应，而用position：absolute则不能。
src https://css-tricks.com/almanac/properties/f/float/

｝
gulp{
src https://markgoodyear.com/2014/01/getting-started-with-gulp/
}
sublime{
折叠shortcut command＋opt＋［
取消折叠shortcut command＋opt＋］
src https://www.youtube.com/watch?v=80UUPMUAP-g
增加／减少数字
每次1 ctrl＋方向键
每次0.1 option＋方向键
每次10 command＋option＋方向键
}
iterm{
I am used to command + k to clear up the terminal in mac. It is very helpful specially while debugging through server logs.
}
rvm{
RVM is a command-line tool which allows you to easily install, manage, and work with multiple ruby environments from interpreters to sets of gems.
}
cmake{
 -D <var>[:<type>]=<value>    = Create a cmake cache entry.
 eg cmake -D CMAKE_BUILD_TYPE=RELEASE -D CMAKE_INSTALL_PREFIX=/usr/local ..
  cmake -D BUILD_EXAMPLES=ON ..... !important
  -G <generator-name>          = Specify a build system generator.
  This module no longer exists.
}
FindVTK{
This module existed in versions of CMake prior to 3.1, but became only a thin wrapper around find_package(VTK NO_MODULE) to provide compatibility for projects using long-outdated conventions. Now find_package(VTK) will search for VTKConfig.cmake directly.
}
vtk{
可以在main函数最后加上cin>>i 来让程序不立即结束
｝
future-learn{
 学习snipmate加速CMakeLists.txt的书写
}
sublime{
在安装了hayaku后，用ctrl＋e可以使用emmet的补全
用途 输入m0-auto使用tab不能补全使用ctrl＋e可以补全
zm fold more
zr reduce fold
updatedb mac｛
 The locate.updatedb utility updates the database used by locate(1).  It is typically run once a week by
     the /System/Library/LaunchDaemons/com.apple.locate.plist job.
｝
item{
src:http://foocoder.com/blog/wo-zai-yong-de-macruan-jian.html/
}
python正则表达式{
src:http://www.imooc.com/learn/550
}
opencv{
/usr/local/Cellar/opencv/2.4.12_2/share/OpenCV/OpenCVConfig.cmake
}
